/**
 * Server domain barrel (`@domain`).
 *
 * Add server-specific domain modules here as they appear. Direct imports of
 * `@classicalmoser/prevail-rules` elsewhere in the codebase are intentional
 * and preferred over re-export façades.
 *
 * @see ./README.md
 */

export { UNTITLED_ARMY_NAME, armyDisplayName } from './armies';
